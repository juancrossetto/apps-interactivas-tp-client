import React from 'react'
import ReactDOM from 'react-dom'

const Main = ({ children, extraPadded }) => (
  <main className="Main">
    {children}

   <style jsx>{`
   
   `}</style>
  </main>
);

export default Main;
