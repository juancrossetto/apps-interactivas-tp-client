import React from 'react'
import ReactDOM from 'react-dom'

const Footer = props => (
  <React.Fragment>
    <footer>
      <div className="container">
     
      </div>
    </footer>

    <style jsx>{`
    `}
    </style>
  </React.Fragment>
);

export default Footer;
